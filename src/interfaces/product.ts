export interface ICategory {
  id: string;
  title: string;
  type: string;
}

export interface ISubcategory {
  id: string;
  title: string;
}

export interface IBrand {
  id: string;
  name: string;
  slug: string;
}
